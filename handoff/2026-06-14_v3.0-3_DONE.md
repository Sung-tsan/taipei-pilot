# v3.0-3 機場/城市生活感 + 日夜 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v3.0-3_airport_life_daynight.md`](2026-06-13_v3.0-3_airport_life_daynight.md)。V3 子階段 **3／4**。
> **本階段含 GO/NO-GO：perf（雙視口 60fps / draws < 300）→ e2e 已 assert draws<300（單視口）；雙真機 60fps 仍待 Sung。**

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **313 passed**（28 files；+7：daynight 4、airport-life 3）。
- `npm run e2e` → **23 passed**（+1：夜燈可開 + **draws<300 perf assert**）。

## DoD 對帳
- [x] **靜態擺件清單到位、merged/instanced、draws<300**：`scene/airport-life.js`——停機坪靜態飛機（重用 T-34C voxel 當 prop，4 架不同塗裝）/ 跑道·滑行道燈 / 航廈窗光 / 停機坪泛光燈 / 靜態地面車輛（3 台 voxel）/ 風向袋桿 **全 merged**；夜燈 merged 成 1 mesh。**整個生活感只加 ~4 draws**（測試斷言 group mesh ≤8；e2e 斷言單視口總 draws<300）。
- [x] **日夜開場可選、純氛圍不懲罰、夜燈亮、與天氣協調**：設定頁「🕑 時段：☀️白天/🌇黃昏/🌙夜晚」；`weather/daynight.js`（純參數）+ `WeatherRenderer.apply(type, dayParams)` **compose 天氣×日夜在一次呼叫**（夜＝陽光弱+冷藍光+天空染深藍+夜燈亮）。**純氛圍：日夜只動光/色，不動 fog 遠近 → 夜不降能見度**（除非同時起霧＝weather 管）。
- [x] **風向袋接 v3.0-2 風向**：袋飄向「風吹去」的方向、風速越大越水平（指示功能）；旋轉雷達緩慢掃描。
- [x] **移動 bonus 視 perf 收**：本輪以靜態主軸達標即收（draws 充裕）；移動車流/環境機**未做**（靜態已解「停機坪沒飛機」抱怨 + 留 perf 餘裕）——明示見取捨，非靜默砍。
- [x] vitest + e2e + typecheck 全綠（附 draws 量測 e2e）。
- [ ] **Sung 雙真機 HITL（併 v3.0-4）**：機場有人住的感覺、日夜氛圍、停機坪有飛機（解抱怨）、不掉幀。**dev：設定頁切時段、按 C 切天氣預覽。**

## 套件先行（工法基準）——明示評估與決策
- 通用件（車/航廈/設備）的 **CC0 low-poly GLB 大量引入＝V4**（canon §11：「GLB scale/正規化 spike 併入 v4.0、V4＝首次大量引入 GLB」）。
- v3.0-3 是 **perf GO/NO-GO** 階段 → 用 **merged/instanced voxel（最低 draws）**最穩；停機坪飛機＝重用既有 voxel；地面車輛＝trivial 小 voxel（套件先行允許 trivial 自建 + canon 把 GLB 延到 V4）。
- **已評估** `spike-glb/` 的 Kenney CC0 車：留待 V4 隨民航機/航廈/地勤車一起走 `GLTFLoader` + 正規化 pipeline（不在 perf gate 階段提前引入 async GLB 風險）。→ 這是「為何此階段不用 GLB」的明示理由（接套件先行的「handoff 寫理由」要求）。

## ⚠️ 取捨（明示）
1. **移動車流/環境機未做**（handoff 列「perf bonus、不擋版本」）：靜態主軸已達 DoD + 解抱怨；移動體留後（內容滴）。
2. **GLB 通用件延 V4**（見上，canon 決策）。
3. **日夜不做流動循環**（handoff 拍定 stretch）：開場固定時段。

## 新增/改動檔案
- 新增：`src/display/weather/daynight.js`（純參數）、`src/display/scene/airport-life.js`（生活感擺件）、`tests/daynight.test.js`、`tests/airport-life.test.js`、本 DONE。
- 改：`src/display/weather/weather-render.js`（apply 加日夜 compose）、`src/display/main.js`（AirportLife + dayNight + applyEnv compose + 每幀 update 雷達/風向袋 + 設定時段列 + __tp）、`index.html`（時段設定列）、`e2e/flight.spec.js`。

## 下一步
- v3.0-4 天氣挑戰任務（現有任務型+`weatherRequirement`，台北版 DRAFT prompt）+ 天氣音效（風/雨/霧悶音/亂流抖/日夜底噪，hybrid）+ 整合回歸 → **V3 收尾打 tag `taipei-pilot-v3.0`**（待 Sung 批次 HITL）。
