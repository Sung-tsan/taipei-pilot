# v1.1-0 UI/UX 重新設計 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v1.1-0_ui_redesign.md`](2026-06-13_v1.1-0_ui_redesign.md)。
> 本檔記錄實作結果、測試數、與**需 Sung 知道的兩個取捨**。HITL 手感閘**未自評**，批次留 v1.1-5 Sung 雙真機親驗。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test`（vitest）→ **77 passed**（7 files）。新增：`hud-slots`(4)、`protocol` 擴充(+4)、`flight-model` 複雜操控(+5)。
- `npm run e2e`（playwright）→ **8 passed**。新增：複雜版送 rudder/flaps/trim+yaw、舊式 remote 向後相容、分屏雙 HUD 槽位。
- baseline 之前的 e2e 隔離 flake 已先修（見 `../TEST_BASELINE.md`）。

## DoD 對帳
- [x] `DESIGN_WEB_UI.md` 完成（色票 token / Tactile / haptic 政策 / 6 槽位契約 + 顯隱規則）。
- [x] display 6 槽位框架可運作（`src/display/ui/hud-slots.js` 純契約 + `hud.js` 控制器）；v1 元素歸位：高度→AltBand、機種/起飛提示→ModeSlot、回家箭頭→HomeSlot、事件 toast→CenterSlot（瞬時）；TaskSlot/StatusSlot 空契約。自由飛 e2e 回歸綠。
- [x] remote 簡單版手感不變（既有按鈕 CSS 未動）；context 動作鍵 slot（`context-keys.js`，預設 喇叭/降落輔助，可替換 config）。
- [x] remote 複雜版：scheme 選擇（存 `tp_remote_scheme`）、迷你儀表（空速/高度/航向，走 pstate 擴充）、方向舵 rudder、襟翼 flaps、配平 trim（`complex-controls.js`）。
- [x] 協定向後相容（`in` 加 rudder/flaps/trim、`pstate` 加 spd/alt/hdg，缺值＝中立；舊 remote e2e 仍綠）。
- [ ] **Sung 雙真機 HITL（4 項）批次留 v1.1-5**：①簡單版手感同 v1 ②複雜版 rudder 做得出 crab/協調轉彎 ③兩手機各選 scheme 同時連 ④分屏每半屏 HUD 正常。

## B1 / 工法基準檢查
- 槽位「哪個 mode 亮哪些」集中在 `hud-slots.js` 一處（純函式可測）；新增模式＝改 `MODE_SLOTS` 一張表。
- context 動作鍵、scheme、MAX_FLAPS 單一來源（`MAX_FLAPS` 進 `shared/constants.js`，remote 與 flight-model 共用，不雙寫）。
- 色值/圓角/Tactile/haptic 收斂在 `DESIGN_WEB_UI.md` 的 `:root` token，display 與 remote 同一份。

## ⚠️ 兩個需明示的取捨（沒有悄悄選邊）
1. **flight-model.js 有改（與「硬性約束：不碰 flight-model」字面衝突，但 P4 又要求「rudder→yaw、接 flight-model 最小改動」）。**
   解法：**純加法 + 中立預設（rudder/flaps/trim 缺值＝0）**。已用測試證明中立時與 v1 **位元級一致**（20 個既有 flight-model 測試全綠 + 新增「中立安全」測試逐欄比對 pos/heading/pitch/bank/speed）。即：簡單版/鍵盤/v1 的手感完全沒動，只有複雜版送非 0 值才生效。請 Sung 在 v1.1-5 HITL 確認簡單版手感無感變化。
2. **design 稿微調**：StatusSlot 在 v1.1-0 是**空契約**（原稿一度寫機身識別 chip）；事件 toast 掛 **CenterSlot 當瞬時 overlay**（CenterSlot 持久內容仍空契約）。兩者已回寫進 `DESIGN_WEB_UI.md §5`，與 handoff「TaskSlot/StatusSlot/CenterSlot 先留空契約」一致。

## 新增/改動檔案
- 新增：`DESIGN_WEB_UI.md`、`src/display/ui/hud-slots.js`、`src/display/ui/hud.js`、`src/remote/context-keys.js`、`src/remote/complex-controls.js`、`tests/hud-slots.test.js`。
- 改：`index.html`、`remote.html`、`src/display/main.js`、`src/display/audio.js`（horn）、`src/display/net/client.js`、`src/display/flight/flight-model.js`、`src/remote/main.js`、`src/remote/net/client.js`、`shared/protocol.js`、`shared/constants.js`、`e2e/{connect,flight}.spec.js`、`tests/{protocol,flight-model}.test.js`。

## rudder yaw trace（必要 trace）
複雜版右滿舵（rudder=1）平飛 2s（r=0,p=0,th=0.5）：heading 從 0 → +0.3 rad 以上（e2e poll 取最短角差驗證），左滿舵對稱往負。中立直飛 heading |Δ|<0.05。RUDDER_YAW=0.5 rad/s（手感旋鈕，HITL 可調）。
