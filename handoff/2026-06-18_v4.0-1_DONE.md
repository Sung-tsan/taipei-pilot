# v4.0-1 ATR-72 + 滑行道網路 + 地面導航 + 地面碰撞 — 完成回報（agent 端）

> 對照交付：[`2026-06-14_v4.0-1_atr72_taxiway_ground_nav.md`](2026-06-14_v4.0-1_atr72_taxiway_ground_nav.md)。V4（民航與地面作業）子階段 **1／4**。
> P1+P2、P3 已逐階段 commit（`3b09ca0`、`f209ff7`）；本次 = **P4 地面碰撞 + P5 測試 + 兩輪 HITL 修**，v4.0-1 收尾。

## 三閘（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **336 passed**（31 files；v3.0-4 收尾為 313 → +23：taxiway graph 連通、path-follow 尋路/最近距離、地面碰撞越界、ATR spec 載入、weather pref）。
- `npm run e2e` → **28 passed**（含 #16 ATR GLB 機體載入、#17 地面導航路線+ATC、#18 地面碰撞越界事件）。

## DoD 對帳
- [x] **ATR-72 spec（含 wingspan/minRunwayLength）** — `plane-specs.js` `atr72`：重/轉彎慢/起飛滑行長、`dims.wingspan 27 / minRunwayLength 1200`（短場挑戰 + 迫降共用）、`fuelSec 1500`、`flaps/gear` 分段沿用 P 表。機體＝CC-BY low-poly GLB 過 §11 normalize 管線（**非手刻 voxel**）。
- [x] **松山 taxiway graph + gates（真實結構）** — `scene/taxiway.js`：跑道 10/28、南側平行滑行道、聯絡道、6 登機門；節點/邊純資料（vitest 測連通性）。字母代號仍標「待對 AIP/FlightAware 微調」。
- [x] **地面導航三合一** — `scene/ground-nav.js` + `path-follow.js`：跟我車（Kenney CC0）沿 `arrivalRoute` 帶路 + 綠中線燈高亮 + ATC 文字框架（phraseology 內容留 v4.1-2）。
- [x] **P4 地面碰撞 / 越界** — `main.js updateGroundNav()`：`groundNav.offRouteDistance(p) > 55m` → **真實模式接 `damagePct`（−10%）**、安全/溫和模式 toast「回到綠線」+ bump 音；per-slot 1.5s 冷卻（不每幀累加）。撞機在 `plane-collision`；地勤車碰撞留 v4.1-1。
- [x] **滑行道鋪面** — `scene/airport.js`：沿 graph 每條非跑道邊鋪 Lambert 鋪面帶、`mergeGeometries` 併 1 顆（perf），讓綠線/跟我車落在鋪面上。
- [x] **§11 共存** — ATR/跟我車＝CC0/CC-BY low-poly 過 normalize recipe；既有 voxel（T-34C/F-16/城市/機場）全留。
- [x] **vitest + typecheck 全綠**（上方附數）。
- [ ] **Sung 雙真機 HITL（併 v4.1-2）** — ATR 飛/滑手感、導航跟得上、CC0 件與 voxel 不違和。本輪已先收兩個 HITL round（見下），完整 HITL 仍待。

## 兩輪 HITL 修（2026-06-16，Sung 真機）
1. **ATR 機鼻朝後** → `plane-specs atr72.model.yaw = Math.PI`（轉 180°）。
2. **ATR 轉向太鈍** → `MAX_BANK 0.6→0.95、BANK_RATE 1.3→2.6、MAX_PITCH 0.3→0.35、PITCH_RATE 0.8→1.1、TURN_G 9→13`（仍比 F-16 重穩）。
3. **綠線差一個跑道在旁邊** → 改從「最近節點（含跑道門檻）」起、且綠線從玩家機身正下方起頭（`nearestNode` 取代 `nearestExit`、route 前插 `{p.x,p.z}`）。
4. **雨關不掉** → 新增天氣偏好鎖定（settings.weather：auto/晴/多雲/雨/霧；`auto` 才照後果模式 roll）。`index.html` 天氣列 + `settings-store` `WEATHER_PREFS`。

## ⚠️ 取捨（明示，非降級；已 append POLISH_BACKLOG）
1. **GLB 民航機起落架恆放下** — 現役 `airliner.glb`（CC-BY）把 3 根支柱烤進 `Airplane1` 單一 mesh、無法單獨隱藏 → 視覺不收放；「收輪飛較快」仍由 flight-model `gearDown` 速度上限生效。真收放需 separable-gear 或無支柱模型。
   - **後續**：2026-06-18 已 sourcing 4 機型 clean-belly/無支柱候選（見 `public/models/candidates/CANDIDATES.md`），待拍板換入。
2. **ATR 外型為代用**（CC-BY 噴射客機，非雙螺旋槳）— 玩法（滑行/靠橋/地面作業）不受影響；線上查無 CC0 客機（見 CREDITS.md / CANDIDATES.md）。
3. **ATC 文字＝框架**，真 phraseology 內容留 v4.1-2。

## 新增/改動檔案
- 改：`plane-specs.js`（ATR HITL 轉向 + yaw=π）、`plane-entity.js`（起落架註解明示限制）、`scene/airport.js`（taxiway 鋪面 merge）、`scene/ground-nav.js`（offRouteDistance）、`scene/path-follow.js`（nearestDistance）、`scene/taxiway.js`（nearestNode）、`main.js`（P4 越界 + 天氣偏好 + e2e hooks）、`ui/settings-store.js`（WEATHER_PREFS）、`index.html`（天氣列）、`POLISH_BACKLOG.md`（取捨 harvest + 維度10）。
- 測：`tests/path-follow.test.js`、`tests/settings-store.test.js`、`e2e/flight.spec.js`、`e2e/connect.spec.js`。
- 新：本 DONE。

## 之後
- **v4.0-2（到場流程 / arrival flow）** ＝下一階段（handoff `2026-06-14_v4.0-2_arrival_flow.md` 已拆）。
- 並行小修：飛行模型「重力耦合（俯衝快於爬升）+ 失速 Stall」（Sung 2026-06-18 指示）。
- 待 Sung：v4.0-1 完整雙真機 HITL（其餘已收兩輪）；4 機型候選換入拍板。
