# v5.0-1 DONE — 台灣全圖 + 航線引擎 + airport-template

> 北極星 V5 子階段 1／4。對應 handoff `2026-06-14_v5.0-1_taiwan_map_route_engine.md`。
> 三閘全綠：typecheck 0 / **381 unit** / **34 e2e**（baseline 362/31 + 本階段 +19 unit +3 e2e）。

## 交付（DoD 對照）

- [x] **airport-template 參數化**：`scene/airport-scene.js` `makeAirportScene(id)`——松山(tsa) 委派既有 `makeTaipei()`（手刻城市/三河/七地標完全不動，v1 美術零 churn）；其餘機場走參數化 template（跑道依 spec 長/向、航廈/塔台、滑行道、地形(島/海岸/山)、招牌地標）。每機場以自己跑道中心為世界原點。
- [x] **松山/高雄/金門 3 airspace 可飛**（demo）：e2e 實證松山→高雄、松山→金門切換成功降落點。其餘 6 場 template 已可建、`DEMO_AIRPORTS` 閘控待 v5.1-2 解鎖。
- [x] **台灣全圖 2D 選線**：`scene/airports.js`（9 機場真實經緯度 + 9 航線 + haversine 航程 + `mapXY` 投影）。`index.html` `#routeMap` SVG 全圖（台灣本島輪廓 + 九機場真實相對位置＝地理教材），點綠色機場選航線 → 出發。
- [x] **雲上巡航 + 時間壓縮 + 半自動 + 切換**：`missions/route-engine.js`（純狀態機 climb→cruise→descent；`cruiseDuration` 把航程壓到 22–48 秒；cruise 段 progress 自動推進、玩家可微調航向＝半自動）。起飛偵測選定航線 → `beginCruise`；progress→1 → `arriveCruise` → `loadAirport(dest)` + 飛機放最終進場 → 玩家降落目的地。
- [x] **機場 props/通用件＝voxel template、招牌地標＝手刻 voxel（§11）**：航廈/塔台/滑行道通用件；每場一座招牌地標（高雄港吊車/金門風車/南竿燈塔…）手刻 voxel。CC0 low-poly GLB 通用件正規化管線沿用 V4（v5.1-2 機隊用）。
- [x] vitest + e2e + typecheck 全綠（見下）。
- [ ] **Sung 雙真機 HITL（本階段優先 GO/NO-GO）**：全圖選線→巡航→到達順、不暈、3 機場可飛、金門霧招牌有感 → 併 v5.1-2 批次驗。

## 新檔 / 改檔

| 檔 | 內容 |
|---|---|
| `src/display/scene/airports.js`（新） | 9 機場 registry（經緯度/跑道/weather key/terrain/tier/招牌）+ 9 航線（覆蓋全 9 場，fact DRAFT）+ haversine + `routeDistanceKm` + `mapXY` 投影 + `routesFrom`(雙向)/`routeOtherEnd` |
| `src/display/missions/route-engine.js`（新） | 巡航狀態機（純）：時間壓縮、climb→cruise→descent、半自動到達精準度、ETA |
| `src/display/scene/airport-scene.js`（新） | `makeAirportScene(id)`：tsa→makeTaipei；template→跑道/航廈/滑行道/地形/招牌地標 + env/terrainAt/solidAt + runwayDir/taxi/spawnPose/dispose |
| `src/display/scene/taxiway.js` | `makeTaxiwayGraph(runwayLength=2605)` 參數化（依跑道長等比，結構/連通不變）；松山預設向後相容 |
| `src/display/weather/weather.js` | 補 `tpe`（桃園）profile（航網第 9 key；draft:true） |
| `src/display/main.js` | 多機場狀態（`air`/`env`/`taxi`/`RWDIR`/`labels` per-airport + cache）；`loadAirport` 切換；cruise 流程（beginCruise/arriveCruise/placeOnApproach/updateCruiseStep）；全圖選線 UI；cruise overlay；RUNWAY_DIR→RWDIR、spawnPose→air.spawnPose、taipei.→air. 全面改 |
| `index.html` | `🗺` 鈕 + `#routeMap` 全圖 modal（SVG）+ `#cruiseOverlay` 巡航進度條 + CSS |
| `tests/airports.test.js`（新，12） | registry/航線/投影/與 weather 對齊 |
| `tests/route-engine.test.js`（新，7） | 時間壓縮/狀態機/精準度/ETA |
| `e2e/flight.spec.js` | +3：松山→高雄全圖選線巡航切換、航線圖 UI、松山→金門離島切換（皆無 console error） |

## 三閘輸出
- `npm run typecheck` → 0 errors
- `npm test` → **381 passed**（35 files）
- `npm run e2e` → **34 passed**（含全部既有 V1–V4 回歸＝多機場重構零回歸）

## 明示取捨（非降級；harvest 進 POLISH_BACKLOG / 待 Sung）
1. **route facts + airport facts 標 DRAFT**（教訓 B5）：9 航線 fact + 招牌地標名待 Sung 校稿（v5.1-2 彙整）。
2. **ATC 站名仍 hardcode「松山」**：遠端機場地面 ATC 會講松山 → v5.1-1 把 ATC 改機場感知（同一交付下一階段修）。
3. **AirportLife（停機坪生活感）仍在世界原點共用**：各機場原點都會出現松山風的停機坪擺件 → 視覺輕微不合，記 POLISH_BACKLOG（v5.1-2/V6）。
4. **招牌地標為手刻 voxel 小件**（識別用）：非精細地標；離島/山景為程序化（海環/山丘 merge）。
5. **巡航＝半自動快轉**（cruise 段 autopilot 平飛 + 玩家微調航向）：刻意不渲染跨海 300km（北極星拍板）。暈不暈 GO/NO-GO 待 HITL。
6. **mission/dogfight/race 仍以松山為錨**（home 模式）；航線飛行是 free/任務的疊加活動。遠端機場玩 home 模式非本階段範圍。

## 下一步
v5.0-2：油量機制（啟用 `fuelSec`）+ 油盡→迫降（真實，接 `onFuelEmpty`）+ 載客/準點航班任務 + 航網收集（飛過航線點亮、九航線全通大慶祝）。
