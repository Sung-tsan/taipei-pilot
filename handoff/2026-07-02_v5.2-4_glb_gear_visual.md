# v5.2-4 GLB 民航機起落架視覺收放

> 台北小飛官 V5.2 polish 子階段 **4／4**（源自 2026-07-02 全面盤點；HITL 回報「機輪收不起來」）。前置：v5.2-3 全綠。

## §0-PRE
- [ ] baseline 綠燈：`npm test` + `npm run typecheck` 全綠。
- [ ] 最大未知數：GLB 內起落架是否可用 node 名/幾何位置辨識出來（GO/NO-GO：spike 腳本列出各 GLB scene graph——找得到獨立 gear node 走方案 A，找不到走方案 B）。

## 設計哲學
- 物理早就對了（收輪飛更快），欠的只是眼睛的反饋。孩子按 G 要「看得到」輪子收進肚子。

## 硬性約束（拍定）
- **方案 A（優先）**：`glb-model.js` 載入後以 node 名（/gear|wheel|lg/i）＋幾何啟發（machine-belly 下方小件）辨識 gear node → `plane-entity.js` 掛進既有 `this.gear` 收放邏輯（visible + scale.y，複用 voxel 機那套）。
- **方案 B（A 不可行時的正式 fallback，非降級）**：GLB 機腹疊「手刻 voxel 假起落架」（複用 t34c gear 定義縮放），`gearDown` 收放假輪、GLB 原生輪恆藏不掉就標記回 POLISH_BACKLOG 維度 1（真解＝clean-belly GLB 資產）。
- voxel 機（T-34C/F-16）既有收放**一行不動**。
- 地面強制放下、V_MAX_GEAR 速度上限機制不動。

## 既有資產照抄
- gear 收放動畫（`_gearK`/expDamp/GEAR_TOP）：plane-entity.js 既有。
- t34c gear voxel 定義：`src/voxel/models/t34c.js` 既有。
- GLB normalize recipe：`spike-glb/` 既有。

## 分 Phase 實作
**P1 — spike**：腳本 dump 三個 GLB 的 scene graph → GO/NO-GO 拍板 A 或 B（結果寫進本檔）。

> **P1 spike 結果（2026-07-02 實測）**：`atr72.glb` 6 node 全是機身分段（鼻/中/尾/翼/垂尾，無輪組）；
> `a330.glb` 單一 mesh；`candidates/` 五件也無 separable gear（atr72-prop 只分出螺旋槳）。
> 唯一有 Wheel1–6 node 的 `airliner.glb` 已停用（CREDITS.md：露輪問題換掉）。
> 且 CREDITS.md 證實現役兩件皆 **clean-belly（無外露輪）** → **拍板走方案 B**：
> 參數化 voxel 假輪組（`plane-entity._buildGlbGear`，鼻輪+雙主輪、尺寸隨 lengthM 等比、
> 機身抬輪高 gearH=L×0.045）＋既有 scale.y 收放動畫。資產缺口已記回 POLISH_BACKLOG 維度 1。
**P2 — 依拍板實作**；plane-entity 單元測試補 GLB gear 分支。
**P3 — 全綠 + HITL 清單產出**。

## DoD
- [ ] ATR-72/B737/A330 空中按 G：輪子視覺收起/放下，地面恆放下。
- [ ] vitest + typecheck 全綠（附輸出）。
- [ ] **Sung HITL 截圖待辦（不可 proxy）**：
  - [ ] 三機種各一段起飛收輪/放輪進場，視覺無穿模、無殘影
  - [ ] 方案 B 時：假輪與 GLB 造型不違和

## Out of scope
- Blender 重製 GLB / 換 clean-belly 資產（維度 1 資產缺口，另議）。
- 起落架動畫音效（維度 3）。

## 最後叮嚀
- 方案 B 是「明示取捨」不是偷懶：選 B 必須在 POLISH_BACKLOG 維度 1 append 一行資產缺口。
- 「fallback 到 B」前必須先把 P1 spike 真的跑完貼結果，不可未 spike 就下結論。
