# v4.1-2 ATC 仿真 + 無線電音效 + 整合 — 完成回報（agent 端）

> 對照交付：[`2026-06-14_v4.1-2_atc_voice_polish.md`](2026-06-14_v4.1-2_atc_voice_polish.md)。V4 子階段 **4／4**（V4 收尾）。
> P1（ATC phraseology）+ P2（無線電音效）已併 v4.1-1 同批 commit；本 DONE 收 P3 整合 + V4 全鏈確認。

## 三閘（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **359 passed**（atc×4 含 DRAFT 紀律；air-corridor / context-keys / taxiway / flight 既有）。
- `npm run e2e` → **30 passed**（離場/到場/空中走廊全鏈 + 既有回歸）。

## DoD 對帳
- [x] **ATC 仿真 phraseology（中文簡化、DRAFT）** — `atc-phraseology.js`：離場（後推/taxi/排序/起飛許可）+ 到場（脫離/taxi/指派門/靠橋），callsign「小飛官」、點名「松山塔台/地面」、無吉祥物。`ATC_DRAFT=true` 待 Sung 校稿，`atc.test` 守紀律（教訓 B5）。空中走廊用語在 `air-corridor.js`。
- [x] **塔台無線電音效（filter+雜訊+咔）** — `audio.atcRadio()`：短嘶聲（高頻雜訊爆＝band-pass 感底噪）+ 雙咔（收發 squelch、對講機感）；`setAtc(text, radio)` 內容變更才響（計數器不響、避免每幀咔）。
- [x] **一趟完整航班（到場↔離場）串通** — spawn-at-gate → 登機 → 後推 → taxi → 排序 → 起飛 → **空中走廊**（離場爬升→下風→進場下降）→ 落地 → 脫離 → 指派門 → 停妥靠橋。離場/到場/空中三段狀態機互斥銜接（justTookOff/justLanded 交棒）。
- [x] **民航機 unlock 接線（暫可選）** — ATR 在機庫**直接可選**（`plane-specs` unlock 門檻 flightMin30/landings10 為佔位，v1.2 機制未到＝不擋）；符合交付「v1.2 未到則 ATR 暫可選」。
- [x] **回歸無異常 + 新增回歸測試** — 新增 air-corridor/atc/context-keys 測 + 離場/空中走廊 e2e；既有 359 unit / 30 e2e 全綠（含 perf draws、GLB 載入、到場全鏈）。
- [ ] **DRAFT（ATC 用語）Sung 校稿全清** — phraseology + 空中走廊用語為 agent 擬稿（`ATC_DRAFT=true`），待 Sung 讀給孩子校。
- [ ] **Sung 雙真機 HITL 全項 → V4 出貨、打 tag `taipei-pilot-v4.0`** — agent 不代簽/不代打 tag。HITL 清單見下。

## ⚠️ 取捨（明示）
1. **ATC＝文字 + 合成無線電 squelch**；真·取樣語音 clip／TTS 留 V5（交付明訂 V4＝文字+短clip、V5 才生成 TTS）。合成 squelch 給「對講機感」，非真人語音。
2. **ATC 用語 DRAFT**：擬真+6歲懂兩者我盡量兼顧，但未經 Sung 校稿，視為草稿。
3. 見 v4.1-1 DONE 取捨（乘客計數器、可見環境離場機未做、走廊不強制高度、地勤車重用 sedan 造型）。

## V4（民航與地面）全部實作完成 🎉
- **v4.0-1 ATR+taxiway+地面導航+碰撞 → v4.0-2 到場(落地→脫離→指派門→停妥靠橋) → v4.1-1 離場(spawn-at-gate→登機→後推→taxi→排序→起飛)+空中走廊 → v4.1-2 ATC仿真+無線電音效+整合** 全部 agent 實作完成、逐階段 commit、三閘全綠。
- **一趟完整航班可飛**：gate → 天空 → gate（離場+空中+到場三段串通）。
- **Sung HITL 待驗（全項，agent 不代簽）→ 過則打 tag `taipei-pilot-v4.0`**：
  - [ ] flight：重力/失速手感（gravity/stall，POLISH_BACKLOG §4 旋鈕）
  - [ ] 離場：登機/pushback 懂、遙控器確認鍵順手、起飛排序排隊感
  - [ ] 空中走廊：離場/進場穿越環跟得上、ATC 指引懂
  - [ ] 到場：6 歲跟導航 taxi 到指派門、停妥靠橋
  - [ ] ATC 仿真用語聽得懂（DRAFT 校稿）、無線電音效有感
  - [ ] 一趟完整航班順、地面碰撞受損合理、不掉幀
  - [ ] 4 機型候選（A330/ATR/F16/T34）換入拍板（`public/models/candidates/`）
- 之後＝V5（航線/A→B、離島短場×ATR×霧 大魔王、台灣地圖）。
