# v3.0-4 天氣挑戰任務 + 音效 + 整合 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v3.0-4_weather_missions_audio.md`](2026-06-13_v3.0-4_weather_missions_audio.md)。V3 子階段 **4／4**（V3 收尾）。
> v3.0-1~3 Sung HITL 已 PASS（含 v3.0-2 暈感 + v3.0-3 perf 兩個 GO/NO-GO）。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **313 passed**（28 files；missions DRAFT 紀律測更新涵蓋天氣任務）。
- `npm run e2e` → **24 passed**（+1：天氣挑戰任務覆寫天氣、結束回環境）。

## DoD 對帳
- [x] **天氣挑戰任務（現有任務型 + 條件，台北版 DRAFT prompt）**：airspace 加 2 個——`weather_fog_landing`（起降練習 + `weatherRequirement:'fog'`「霧中降落松山」）、`weather_rain_keelung`（穿圈 + `weatherRequirement:'rain'`「雨中沿基隆河」）。**不新增任務型**（沿用 TAKEOFF_LANDING / RING_ROUTE）。main `refreshMissionWeather()`：當前任務帶 weatherRequirement → 覆寫天氣（roll 之上），任務結束 → 回環境 roll 天氣。每幀檢查、只在變更時重套（不閃）。
- [x] **天氣音效全到位（hybrid、分級）**：`audio.setWeather(type, night)`——雨聲 loop（高帶通噪＝雨絲，雨天開）、側風底噪分級（雨>霧/雲>晴，疊在既有速度耦合風聲上）、夜/黃昏環境底噪（夜蟲感）、霧＝風聲底拉低（悶）。`applyEnv` 一併呼叫 → 天氣/時段切換音效跟著變。
- [x] **天氣系統整合進玩法、回歸無異常、perf 疊加達標**：天氣接自由飛/任務（refreshMissionWeather 每幀；非任務模式＝環境天氣）；天氣切換不破壞既有手感（wind/gust 只真實模式、純加法）；既有 313 unit + 24 e2e 全綠（含 perf draws<300 仍過）。
- [ ] **DRAFT（天氣任務 prompt）Sung 校稿全清**：2 個 prompt 標 `draft:true` 待校（教訓 B5）；`missions.test` 守「天氣任務 prompt=draft、其餘 v1.1=定稿」。
- [ ] **Sung 雙真機 HITL 全項 → V3 出貨、打 tag `taipei-pilot-v3.0`**：台北天氣好看/霧中導航、側風亂流不暈/受損%、生活感+日夜+不掉幀、天氣挑戰任務可玩、天氣音效到位。

## ⚠️ 取捨（明示）
1. **天氣挑戰任務「覆寫」天氣不受後果軸閘**：環境 roll 受後果閘（安全恆晴…），但**玩家主動接的天氣挑戰任務直接強制該天氣**（挑戰是重點）。若希望天氣任務也只在非安全模式出現＝可加 gate（tuning）。
2. **天氣音效＝hybrid 合成**（雨/風/夜底噪/霧悶）：雨聲+側風底+夜底噪清楚；亂流抖音未做獨立層（鏡頭晃 + gust 已給體感）；霧悶以「風聲底拉低」近似。真·取樣 clip 同空戰爆炸＝資產軌待 CC0 drop。
3. **DRAFT prompt 待校稿**：2 條天氣任務 prompt 是我擬的草稿，待 Sung 校。

## 新增/改動檔案
- 改：`src/display/missions/airspace-taipei.js`（2 天氣挑戰任務 + DRAFT prompt）、`src/display/audio.js`（雨/夜底噪 loop + setWeather）、`src/display/main.js`（refreshMissionWeather 覆寫 + applyEnv→audio.setWeather + ambientWeather）、`tests/missions.test.js`（DRAFT 紀律更新）、`e2e/flight.spec.js`、本 DONE。

## V3 全部實作完成 🎉
- **v3.0-1 天氣核心 → v3.0-2 側風亂流+受損% → v3.0-3 生活感+日夜 → v3.0-4 天氣任務+音效 全部 agent 實作完成、逐階段 commit、三閘全綠。**
- **待 Sung：v3.0-4 DRAFT prompt 校稿 + v3.0-4 雙真機 HITL（其餘 v3.0-1~3 已 PASS）→ 打 tag `taipei-pilot-v3.0`**（agent 不代簽/不代打 tag）。
- 之後＝V4（民航與地面，4 份 handoff 已拆；§11 GLB 正規化 spike 在 v4.0 開頭＝首次大量引入 CC0 GLB）。
- 已知資產軌缺口（非程式）：擬真爆炸/雨霧的真·取樣音效 clip（待 CC0 drop）。
