# v5.1-1 DONE — 真 ATC（預生成 grounded bank）

> 北極星 V5 子階段 3／4。對應 handoff `2026-06-14_v5.1-1_atc_grounded_bank.md`。
> 三閘全綠：typecheck 0 / **403 unit**（+11）/ **38 e2e**（+1）。**offline-LAN 不破：runtime 零雲端請求。**

## 交付（DoD 對照）

- [x] **build-time 生成工具**：`scripts/gen-atc-bank.js`（dev-only，遊戲 runtime 永不執行）——raw `fetch` 呼叫 Claude API（`claude-haiku-4-5`，handoff 指定便宜快；引用 claude-api skill 確認 model/params + `output_config.format` JSON schema）；grounded prompt（只准真實航管用語 + 該機場真實 data + 限定 placeholder）；產出過 `validateVariant`（結構/grounding）丟不合格。**不裝 @anthropic-ai/sdk → 遊戲 deps 維持零 AI 套件（offline 不破）。**
- [x] **三件套（教訓 B4/B5）**：① grounded prompt（gen 時）② 結構/grounding 驗證 `validateVariant`（必含階段關鍵詞 + `{station}` + `{cs}` + 無未知 placeholder；build + runtime 同一把尺）③ **固定 clip 地板** `atc-phraseology.js` FLOORS（每階段一句 deterministic、機場感知）④ runtime 缺變體/填不全 → fallback 地板。
- [x] **runtime 挑變體 + 地板 fallback**：`atc-bank.js`（純邏輯）`pickVariant` 從 bank 挑合格變體填機場 data；缺/不合格/殘留 placeholder → null → `atcLine` 退固定地板。**零雲端**（只讀烤好的常數）。
- [x] **ATC 機場感知**：`atc-phraseology.js` 站名走 `air.spec.name`（不再 hardcode 松山）；main.js 9 處 ATC 呼叫傳入目前機場名。e2e 實證：高雄離場 ATC 講「高雄」不講「松山」。英文 TTS「Welcome to Songshan」改通用詞（避免唸錯城市；其餘英文 TTS 在地化記 POLISH_BACKLOG/V6）。
- [x] **Sung 校審**：`ATC_BANK_DRAFT=true` + `ATC_DRAFT=true`（地板 + 變體皆 DRAFT，待 Sung 校審/烤定稿；教訓 B5）。bank 為 hand-authored grounded 種子（＝生成器 + Sung 審後會產出的內容），Sung 可跑 `gen-atc-bank.js` 擴充/重生。
- [x] vitest + e2e + typecheck 全綠。
- [ ] **Sung HITL/校審**：ATC 變體品質、grounding（不亂講）、適齡、無線電感（併 v5.1-2 批次）。

## 新檔 / 改檔

| 檔 | 內容 |
|---|---|
| `src/display/scene/atc-bank.js`（新） | ATC_BANK（8 階段 ×3 grounded 變體，DRAFT）+ ATC_STAGES + validateVariant（三件套結構驗證）+ fillTemplate + pickVariant + ATC_BANK_DRAFT |
| `src/display/scene/atc-phraseology.js` | 重構：機場感知（station 參數）+ FLOORS 固定地板 + atcLine（bank-or-floor）；具名函式接 station（缺省松山 back-compat） |
| `scripts/gen-atc-bank.js`（新） | build-time 生成器（Haiku + grounded prompt + JSON schema + validateVariant 丟不合格）；dev-only、不在 tsconfig、遊戲不連雲 |
| `src/display/main.js` | 9 處 ATC 呼叫傳 `air.spec.name`；英文 TTS welcome 去城市名 |
| `tests/atc-bank.test.js`（新，9） | bank 完整性/驗證/挑變體/地板 fallback |
| `tests/atc.test.js` | 改：grounding 不變式（站名+呼號+關鍵詞，多抽涵蓋變體）+ 機場感知 |
| `e2e/flight.spec.js` | +1：高雄離場 ATC 機場感知 |

## 三閘輸出
- `npm run typecheck` → 0 errors
- `npm test` → **403 passed**（37 files）
- `npm run e2e` → **38 passed**（含全部既有回歸）

## 明示取捨（harvest 進 POLISH_BACKLOG / 待 Sung）
1. **bank 為 hand-authored grounded 種子（DRAFT）**：因本機無 ANTHROPIC_API_KEY 且 runtime 須 offline，無法在此跑生成器。提供完整 `gen-atc-bank.js`（Sung 有 key 時可跑擴充/重生）+ 一份已 grounded/適齡的種子 bank，標 DRAFT 待 Sung 校審烤定稿。**非降級**：三件套地板 + 驗證 + 零雲端全到位。
2. **gen 用 raw fetch（非 SDK）**：刻意不引入 @anthropic-ai/sdk，保遊戲 deps 零 AI 套件（offline-LAN 鐵律）。claude-api skill 預設用 SDK，此處有「離線遊戲 deps 純淨」的明確理由走 raw HTTP。
3. **英文 TTS 仍部分松山中心**（跑道號 one zero、climb/final 等通用語）：顯示 ATC（中文 bank）已機場感知；英文 TTS 全在地化（各機場跑道號英文）記 POLISH_BACKLOG（V6）。
4. **bank 每階段 3 變體**：足夠不重複感；Sung 跑生成器可擴更多。

## 下一步
v5.1-2（V5 收尾）：機隊 B737 + 其餘 6 機場 rollout + V3 招牌天氣活化 + perf 回歸 + DRAFT 彙整 + ROADMAP/baseline/記憶更新 → 待 Sung 批次雙真機 HITL → tag `taipei-pilot-v5.0`（＝北極星全達成）。
