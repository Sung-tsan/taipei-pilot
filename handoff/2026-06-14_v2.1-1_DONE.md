# v2.1-1 競爭任務賽（競速）— 完成回報（agent 端）

> 對照交付：[`2026-06-13_v2.1-1_race_mode.md`](2026-06-13_v2.1-1_race_mode.md)。V2 子階段 **6／6**（V2 收尾）。
> race 規則核心（`missions/race.js`）平行預建已測；本檔記錄整合 + 起點/終點視覺（Sung HITL #4）。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **281 passed**（race 核心 17 測已綠；本階段整合無新純邏輯，靠 e2e 驗接線）。
- `npm run e2e` → **20 passed**（+1：選競速 → 賽道標記建出、切穿圈/地標兩型）。

## DoD 對帳
- [x] **兩型競速可玩**：①穿圈航線（沿基隆河佈 4 個金圈、依序穿、最後綠圈＝終點）②地標衝刺（衝到 101 綠圈）。選單 `#raceRow` 即時切。
- [x] **名次＝完成時間；雙人各自獨立計時**：各自「起飛即開始計時」（`racer.startAt`）；名次由 `ranking()` 依 finishTime 升序。
- [x] **起點/終點呈現（HITL #4：要知道從哪飛到哪）**：3D 賽道標記 `race-markers.js`——**起點閘門（青）+ 航圈（金）+ 終點圈（綠）**，每圈軸朝上一點（飛起來像穿門），過圈淡化、輕微脈動；**每位玩家 TaskSlot 有計時 + 進度（第 n/N 圈）+ 指向自己下一目標的箭頭**。
- [x] **落後不淘汰、完賽都報名次**：完賽報「第 N 名 + 秒數」（友善：落後也有名次）；全員完賽放煙火慶祝。
- [x] vitest（競速判定/計時/名次/兩型，race 核心 17 測）+ e2e（兩型賽道建出）+ typecheck 全綠。
- [ ] **Sung 雙真機 HITL**：兩型競速好玩、落後方不過度挫折、名次清楚（併 V2 批次 HITL）。

## ⚠️ 取捨（明示）
1. **「完賽都點亮收集」以慶祝呈現，未寫進收集簿**：完賽有名次 toast + 全員完賽煙火；但收集簿（`collection.lit`）是地標導向，競速完賽未寫入一筆正式記錄。屬輕度簡化（慶祝已到位），正式「競速成就」收集留後。
2. **賽道過圈淡化用「領先進度」**（雙人共享同一條 3D 賽道）：個別玩家的「下一個目標」靠各自的 TaskSlot 箭頭精準導引；3D 圈淡化只反映領先者，避免雙人不同進度時賽道閃爍。
3. **穿圈判定為 2D 水平半徑**（race.js 沿用 v1.1 幾何，高度不卡，6 歲友善）：飛到圈的水平範圍內即算穿過，不必精準對準圈洞。

## 新增/改動檔案
- 新增：`src/display/missions/race-markers.js`（賽道 3D 標記）、本 DONE。
- 改：`src/display/main.js`（race 狀態 + buildRaceCourse 兩型 + setRaceActive + updateRace 計時/穿圈/名次 + raceHudText + 賽道型選單 raceRow + __tp.race）、`index.html`（`#raceSection` 賽道型選單 + CSS）、`e2e/flight.spec.js`。

## V2 全部實作完成 🎉
- **v2.0-1（模式選單+F-16）→ v2.0-2（武器）→ HITL 修正 → v2.0-3（氣球計分+PvP）→ v2.0-4（敵機+紅區，含難度 HITL 調整）→ v2.0-5（空戰 UI/Polish）→ v2.1-1（競速）全部 agent 實作完成、逐階段 commit、三閘全綠。**
- **待 Sung 批次雙真機 HITL 全 V2**（空戰各子模式手感/難度、PvP 手足互動、競速兩型、UI 可讀、紅區沒誤傷地標）→ 全過後**打 tag `taipei-pilot-v2.0`**（agent 不代簽 HITL/不代打 tag）。
- 已知資產軌缺口（非程式）：擬真爆炸真·取樣音效 clip（待 CC0 音效 drop）。
