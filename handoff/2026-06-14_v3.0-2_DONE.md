# v3.0-2 側風/亂流 + 受損百分比 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v3.0-2_crosswind_turbulence_damage.md`](2026-06-13_v3.0-2_crosswind_turbulence_damage.md)。V3 子階段 **2／4**。
> **本階段含 V3 最大未知數 GO/NO-GO：暈感 → 仍待 Sung 雙真機親驗**（鐵律 2）。幅度寧可先弱（減暈），HITL 再加。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **306 passed**（26 files；+15：wind-damage 5、weather +2、consequence +4 damagePct、flight-model +3 wind/gust、settings-store +1 camShake）。
- `npm run e2e` → **22 passed**（+1：真實+雨→側風進輸入、鏡頭晃動可關）。

## DoD 對帳
- [x] **flight-model 外力輸入（側風持續側向力 + 亂流擾動），只真實模式**：`Input` 加 `wind:{x,z}`（水平側移，需 crab 抵銷）+ `gust:{roll,pitch}`（疊姿態目標）。**純加法+中立預設**：缺省 0 → 與 v1 逐位元一致（回歸測試證實）。main 只在「真實模式 + 飛行中 + 有風」餵非 0。
- [x] **風箭頭 + 風向袋邏輯；亂流鏡頭微晃（極輕、可關）**：HUD AltBand 加 `🌬<箭頭>風速`（箭頭指風來向相對機頭）；風向邏輯（`WIND_FROM_DEG`）供 v3.0-3 風向袋模型同步；chase-cam 加 `shake` 參數（亂流時極輕晃 ≤0.012rad）+ **設定頁「🎥 亂流鏡頭晃動 開/關」**（持久化，減暈鐵律＝可關）。
- [x] **受損百分比**：`consequence.js` 加 `damagePct 0..100`；`deriveDamage`（≥40% 冒煙、=100% 毀）；`addDamagePct`（天氣連續累加路徑，只真實模式）；`registerMishap` real 同步 damagePct（碰撞/天氣/迫降走同一條 damage）。側風受損% ∝ 偏離跑道中線+接地超速（`crosswindDamagePct`）；亂流受損% ∝ 姿態甩出安全包絡（`turbulenceDamagePct`）；皆「容差/包絡內＝0（磁吸接住）」，超過才咬。
- [x] vitest + e2e + typecheck 全綠（附輸出 + 側風→偏移 trace）。
- [ ] **Sung 雙真機 HITL（本階段優先 GO/NO-GO）**：暈不暈、側風 crab 做得到（複雜版 rudder 剛好用上）、亂流是挑戰非挫折、受損% 體感合理。**不暈/想更刺激就調 `weather.js weatherForces`、`main gustAt 幅度`、`chase-cam shake`。**

## 套件先行（工法基準）
- 本階段無新 3D 系統（純飛行物理 + 受損邏輯 + 既有相機微調）→ **零新 dep**，純加在既有 flight-model/consequence/chase-cam。亂流用「時間平滑噪音」（sin 疊加、非亂數→可重現流暢抖動），非粒子/shader。

## ⚠️ 取捨（明示）
1. **風向固定 `WIND_FROM_DEG=20`（台北版，HITL 可調）**；島嶼強側風（澎湖招牌）數值 V5。
2. **風向袋「模型」＝v3.0-3**（本階段只做風向邏輯 + 畫面風箭頭）。
3. **暈感寧可先弱**：gust 幅度 roll≤0.16/pitch≤0.07 rad、鏡頭晃 ≤0.012 rad 且可關。台北亂流多半被磁吸接住、少觸發受損（符合「真實模式才有牙齒、但很輕」）。
4. **damage 同一條**：碰撞(registerMishap)/天氣(addDamagePct)/迫降(v1.1-2 可接)共用 damagePct → smoking/destroyed 一致派生（接 handoff「介面改動讓迫降也接得上 %」）。

## 新增/改動檔案
- 新增：`src/display/flight/wind-damage.js`（受損% 純函式）、`tests/wind-damage.test.js`、本 DONE。
- 改：`src/display/flight/flight-model.js`（Input 加 wind/gust + stepFlying 套用）、`src/display/flight/consequence.js`（damagePct + deriveDamage + addDamagePct + registerMishap 同步）、`src/display/weather/weather.js`（weatherForces）、`src/display/render/chase-cam.js`（shake 參數）、`src/display/ui/settings-store.js`（camShake 持久化）、`src/display/main.js`（wind/gust 套用 + 受損 + 風箭頭 + 鏡頭晃 + 設定開關）、`index.html`（鏡頭晃動設定列）、`tests/{flight-model,consequence,weather,settings-store}.test.js`、`e2e/flight.spec.js`。

## 必要 trace
- **位元不變**（flight-model 測）：不帶 wind/gust 與帶 0 → pos/heading/bank 逐欄相等。
- **側風偏移**：wind.x=8 飛 3s → pos.x 比無風多偏 >10m（被吹偏東）。
- **受損%**（consequence 測）：real 累加 20%→damage→+25%→smoke(45%)→+60%→destroy(reset 清零)；安全/溫和 addDamagePct 不累加（bounce）。

## 下一步
- v3.0-3 機場/城市生活感 + 日夜（**GO/NO-GO：perf 雙視口 60fps / draws<300**）。靜態擺件用 merged/instanced；通用件（停機坪飛機/車/雷達）依**套件先行**評估 CC0 low-poly。風向袋模型接本階段風向邏輯。
