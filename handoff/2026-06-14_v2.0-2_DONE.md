# v2.0-2 武器系統 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v2.0-2_weapon_system.md`](2026-06-13_v2.0-2_weapon_system.md)。V2 子階段 **2／6**。
> **本階段含 V2 最大未知數 GO/NO-GO：武器手感 → 仍待 Sung 雙真機親驗**（鐵律 2，agent 不代簽）。
> 純邏輯核心（`combat/weapons.js`）於平行預建階段完成並單測；本檔記錄「整合進主迴圈」。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test`（vitest）→ **246 passed**（21 files；baseline 237 → +9）。新增：`dogfight`(7 整合測，node 下用 THREE.Scene 直測)、`protocol`(+2：FIRE/WEAPON_SWITCH 位元 + mode 訊息)。weapons 核心 27 測已在預建階段綠。
- `npm run e2e`（playwright）→ **16 passed**（baseline 15 → +1）：選空戰→氣球靶場 spawn→起飛→按 F 開火→彈藥扣減→HUD 顯示武器。

## DoD 對帳
- [x] **3 彈種 + 鎖定（對空自動/對地對準）+ 射後不理追蹤 + 射程/冷卻**：`weapons.js`（cartoon/aa/ag）；對空＝飛近錐內最近自動鎖定 + homing；對地＝飛到紅區上方對準（彈道導向前方地面點）。
- [x] **彈藥有限 + 冷卻 + 回機場補**：每機三種彈匣，發射扣彈 + 冷卻節流；回機場（離跑道中心 <2km 且落地）自動 reload。
- [x] **地標豁免實測（對地標無效）+ 红線回歸**：`canHitGround` 只有「紅區內且不在地標上」才生效；命中地標 → `exempt` 事件 + HUD「⛔ 地標不能打」。weapons.js 有 load-bearing 回歸（拔掉豁免即紅），dogfight 整合測另驗 exempt 不計分。
- [x] vitest + e2e + typecheck 全綠（附輸出 + 鎖定→命中 trace：見 dogfight 測「對空…追蹤命中」）。
- [ ] **Sung 雙真機 HITL（本階段優先 GO/NO-GO）**：鎖定發射手感、追蹤強度、6 歲覺不覺得好玩/太難 → NO-GO 調 `weapons.js` 的 `homingRate`/`rangeM`/`LOCK_CONE_HALF_RAD`。

## ⚠️ 需明示的取捨（沒有悄悄降級——按鐵律明講）
1. **「命中接後果軸三檔（玩家冒煙/出局）」自然延到 v2.0-3/-4**：後果軸是「玩家**被打**」的後果，需要敵對射手。v2.0-2 的目標是**氣球靶（非敵對）→ 命中＝啵+計分**，沒有對玩家施加後果的來源。PvP（v2.0-3）兩機互打、敵機（v2.0-4）攻擊玩家時才接 `consequenceMode`→smoking/destroyed。**地標豁免红線在本階段就實測**（不延）。weapons 命中事件已備好給後果軸消費。
2. **remote 發射鍵 + 模式同步「提前」做了**（原列 v2.0-5）：要 HITL 武器手感，手機就得有發射鍵。所以本階段加了 `protocol` 的 `mode` 廣播（display→remote）+ remote 依模式換 `context-keys`（空戰＝🔥發射/🎯換武器，走 btn bitmask；發射 momentary 連發、換武器上升緣循環）。v2.0-5 仍做正式 HUD 鎖定框/彈藥儀表呈現。
3. **氣球靶 + demo 紅區提前進場（原 v2.0-3/-4 提供目標）**：武器手感無靶可驗 → 本階段放 8 顆氣球（半數活動靶，用預建 `balloonDriftPos`）+ 一個 demo 紅區地面靶。v2.0-3 做 PvP + 計分(擊落+命中率)呈現，v2.0-4 把紅區場景化進 airspace（含肉眼確認不壓地標）。
4. **HUD 為最小可讀版**：武器+彈藥+分數塞 TaskSlot、鎖定指示「🎯」掛 ModeSlot。v2.0-5 做正式鎖定框(CenterSlot)/彈藥冷卻儀表(StatusSlot)。
5. **對地彈道**：weapons.js 的 `spawnProjectile` 平射（無垂直分量），對地會飛不到地面 → 整合層 `dogfight.tryFire` 把對地彈導向機頭前方地面點（高度越高瞄越遠）。屬 glue，不動已測核心。
6. **爆炸音效暫用合成**（非取樣 clip）：tone ladder 卡通=可愛咻噗、擬真=低頻衝壓 + groundBoom。v2.0-5 接 hybrid 取樣 clip。

## 新增/改動檔案
- 新增：`src/display/combat/dogfight.js`（空戰場景編排：氣球靶場/彈丸/每 slot 武器狀態，接 weapons.js 核心）、`tests/dogfight.test.js`、本 DONE 檔。
- 改：`shared/protocol.js`（BTN.FIRE/WEAPON_SWITCH + `mode` 訊息）、`server/relay.js`（mode 轉發 remotes）、`src/display/net/client.js`（sendMode）、`src/remote/net/client.js`（onMode）、`src/remote/main.js`（依模式換 context 鍵）、`src/remote/context-keys.js`（DOGFIGHT_KEYS 改 btn 位元）、`src/display/input/keyboard.js`（F 發射/Q 換武器）、`src/display/flight/flight-model.js`（Input 加 fire/weaponSwitch，flight-model 忽略）、`src/display/audio.js`（weaponFire/balloonPop/groundBoom）、`src/display/main.js`（Dogfight 實例 + updateDogfight + mode 廣播 + 空戰 HUD + __tp.dogfight）、`tests/{protocol,context-keys}.test.js`、`e2e/flight.spec.js`。

## 必要 trace
- **鎖定→命中**（dogfight 測「對空」）：plane(0,200,0) heading 北、氣球正前 300m → `updateLock`='b0' → `tryFire` → 逐步 `step` → 出 `pop` 事件、`score[0]=1`、氣球 alive=false。
- **红線豁免**（dogfight 測「对地红線」）：ag 瞄地標 → `exempt`、score 不變；瞄紅區地面靶 → `boom`、score+1。

## 下一步
- v2.0-3 氣球靶（固定/活動，計分呈現）+ PvP 對打（命中接後果軸：safe/gentle 冒煙重生、real 出局；計分＝擊落+命中率；非 PvP 維持幽靈穿透）。
- v2.0 出貨不打 tag，待 v2.1 完成一起 `taipei-pilot-v2.0`。
