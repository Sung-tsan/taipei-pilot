# V5 DRAFT 校稿清單（待 Sung HITL 校審 → 清旗標定稿）

> 教訓 B5 紀律：生成/草擬內容標 DRAFT，**agent 不自行清旗標**（不代簽 HITL）。Sung 校審後，把對應
> `draft:true`／`ATC_BANK_DRAFT`／`ATC_DRAFT` 改 false 即定稿（同 v1.1「DRAFT 21 條」、v3.0-4 流程）。
> 校審重點：**適齡（6 歲懂）＋ 正確（不亂講）＋ grounding（真實 data）**。

## 1. 航線 fact（9 條，`src/display/scene/airports.js` ROUTES，`fact.draft:true`）
給孩子的「航線地理小知識」（飛抵點亮、收集簿顯示）。≤20 字、口語。
- tsa→tpe：桃園是台灣最大的國際機場。
- tsa→rmq：台中清泉崗有很長的跑道。
- tsa→khh：高雄小港在海邊，是南部大城。
- tsa→hun：花蓮在山和海中間，風會亂亂吹。
- tsa→ttt：台東靠山，降落要小心側風。
- tsa→mzg：澎湖是離島，風特別大。
- tsa→knh：金門常常起霧，看不清楚跑道。
- tsa→lzn：馬祖南竿跑道很短，又常有霧，最難降落！
- khh→mzg：從高雄也能飛去澎湖，飛機不一定都從台北出發。

## 2. 機場招牌挑戰文字 + 招牌天氣倍率（`airports.js` AIRPORTS）
進場提示 `signature` + V5.1-2 新增的 `windScale`/`turbScale`（招牌天氣活化強度，agent 暫定值待手感校）：
- 桃園 大型國際場・好飛｜台中 長跑道｜高雄 海邊市區
- 花蓮 山海之間・亂流（turbScale 1.6）｜台東 側風・山區進場（windScale 1.4 / turbScale 1.3）
- 澎湖 強側風（windScale 1.9）｜金門 易起霧・常關場｜馬祖南竿 短跑道・霧・地形（windScale 1.3 / turbScale 1.2）
- 跑道諸元（headingDeg/lengthM）為真實近似值，待對 AIP 微調（影響短場挑戰強度）。

## 3. ATC bank 變體（`src/display/scene/atc-bank.js`，`ATC_BANK_DRAFT:true`）
8 階段 ×3 grounded 變體（中文簡化、保真實感）：boardComplete / pushback / taxiToHold / holdShort /
cleared / exit / taxiToGate / docked。每句已過結構驗證（含站名+呼號+階段關鍵詞）。
固定地板 FLOORS（`atc-phraseology.js`，`ATC_DRAFT:true`）同樣待校。
- **可選**：Sung 有 ANTHROPIC_API_KEY 時跑 `node scripts/gen-atc-bank.js` 擴充/重生變體（Haiku；遊戲 runtime 永不連雲）。

## 4. 6 機場天氣機率表（`src/display/weather/weather.js` WEATHER_PROFILES，`draft:true`）
tpe / rmq / khh / hun / ttt / knh / lzn / mzg（+ gni 綠島 bonus）—— 機率值為 DRAFT 佔位，
島嶼 fog 偏高（knh 0.20 / lzn 0.22）為招牌；待實地調校。tsa（松山）已定稿（draft:false）。

## 校審後動作
1. 改 `ROUTES[*].fact` / airport `signature` 文字 → 順手把對應 draft 機制清掉（route fact 加 `draft:false` 或移除 copy 包裝）。
2. 滿意 ATC → `ATC_BANK_DRAFT=false` + `ATC_DRAFT=false`。
3. 滿意 6 天氣表 → 各 `draft:false`。
4. 招牌 windScale/turbScale 手感校（雙真機）→ 調 `airports.js` 數值。
5. 全清後即達成 v5.1-2 DoD「DRAFT 全清」→ 連同雙真機 HITL 全項過 → 打 tag `taipei-pilot-v5.0`。
