# v5.0-2 DONE — 航班任務 + 油量 + 航網收集

> 北極星 V5 子階段 2／4。對應 handoff `2026-06-14_v5.0-2_flight_missions_fuel.md`。
> 三閘全綠：typecheck 0 / **392 unit**（+11）/ **37 e2e**（+3）。

## 交付（DoD 對照）

- [x] **油量機制（啟用 fuelSec）**：`missions/fuel.js`（純）——耗油（隨油門 `burnRate`）、航程 gate（`rangeKm`/`canReach`，K=0.18）、油低警告、油盡一次性。HUD AltBand 油量表：真實＝剩餘%（≤20% 紅字），安全/溫和＝⛽∞（寬鬆）。
- [x] **航程 gates**：全圖選線顯示「⛽ 油量足夠 / 油可能不夠（換大油箱機種）」。教練機 range 162km 飛不到最遠離島；A330 range 540km 全可達（接 v5.1-2 B737/A330 大油箱）。
- [x] **油盡→迫降（真實，接 v1.1-2）**：本地飛行油盡 → `engineOut` 引擎熄火（推力強制 0）→ 滑翔 → 接既有迫降/碰撞系統。長航線（航程不足）油盡 → `ditchCruise` 海上迫降（複用 `judgeForcedLanding(water)`）+ 返出發機場。**安全/溫和不耗油**（6 歲不挫折）。
- [x] **載客 + 準點航班任務**：抵達落地結算「🛬 航班完成！載客 N 人・準點抵達 ⭐」（`paxFor` 機種載客數）。
- [x] **航網收集 + 九航線全通大慶祝**：`collection-store.js` 擴充 `routes` Set + `networkCelebrated`；飛抵航線即點亮（`flyRoute`），收集簿新增「🗺 航網」區（飛過航線 + fact）；九航線全通 → 一次性大慶祝（`triggerCelebration` 改可帶標題/文案，地標/航網共用 modal）。
- [x] vitest + e2e + typecheck 全綠。
- [ ] **Sung 雙真機 HITL（併 v5.1-2）**：油量不挫折（6 歲＝安全∞）、油盡迫降緊張感（真實）、載客準點好玩、九航線全通慶祝。

## 新檔 / 改檔

| 檔 | 內容 |
|---|---|
| `src/display/missions/fuel.js`（新） | 純油量：makeFuel/burn/burnRate/fuelFrac/isLow/refuel/rangeKm/routeFuelCostSec/canReach（RANGE_K/LOW_FUEL_FRAC） |
| `src/display/missions/collection-store.js` | +routes/networkCelebrated 持久化；flyRoute/allRoutesFlown/shouldCelebrateNetwork |
| `src/display/main.js` | 每架 fuel + engineOut + lowFuelWarned；resetFuel（spawn/落地/換機/respawn）；本地耗油+油低警告+油盡熄火；巡航耗油+ditchCruise；arriveCruise 點亮航線+全通慶祝；落地載客結算；HUD 油量表；全圖航程 gate 顯示；收集簿航網區 |
| `index.html` | 收集簿航網區（routeCollectionList/Count）+ netCelebrateReplay + celebTitle/Sub ids + CSS |
| `tests/fuel.test.js`（新，11） | 耗油/航程 gate/油盡一次性/油低 |
| `tests/collection-store.test.js` | +4：航網 flyRoute/全通/網慶祝/持久化 |
| `tests/mission-runner.test.js` | Collection literal 補 routes/networkCelebrated（型別對齊） |
| `e2e/flight.spec.js` | +3：航網收集點亮、真實油盡熄火（+安全⛽∞）、九航線全通大慶祝（seed 8 條飛第 9 條） |

## 三閘輸出
- `npm run typecheck` → 0 errors
- `npm test` → **392 passed**（37 files）
- `npm run e2e` → **37 passed**（含全部既有回歸）

## 明示取捨（harvest 進 POLISH_BACKLOG / 待 Sung）
1. **航網收集在「飛抵」點亮（非落地）**：飛抵目的地 airspace＝飛過該航線即點亮（robust + 可測）；落地另結算載客/準點。符合「飛過的航線點亮」。
2. **油盡熄火 e2e 驗到「engineOut + 油量 0」**：熄火後墜落點地形（草地迫降 vs 撞建築→respawn）屬 v1.1-2，由真機 HITL + forced-landing 單測覆蓋——e2e 不綁 terrain（避免假性紅燈）。
3. **航程 gate 係數 K=0.18**（fuel.js 單一旋鈕）：待 HITL 校「哪台飛得到哪」的手感（目前：教練機近線、ATR 多數離島、A330 全可達）。
4. **載客數 paxFor 為遊戲化固定值**（t34c 2/atr72 60/a330 250…）：非真實載重模型。
5. **route fact（航線教材）仍 DRAFT**：待 Sung 校稿（v5.1-2 彙整）。

## 下一步
v5.1-1：真 ATC 預生成 grounded bank（build-time 生成器 + 三件套 + runtime 挑變體/固定地板；ATC 改機場感知，站名走 airport 不再 hardcode 松山）。
