# v1.1-3 任務框架 + 四型 + 資料 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v1.1-3_mission_framework.md`](2026-06-13_v1.1-3_mission_framework.md)。
> 本階段＝**純邏輯/資料，UI 在 v1.1-4，無獨立 HITL**（併 v1.1-5）。

## 三閘全綠
- `npm run typecheck` → **0 errors**。
- `npm test` → **117 passed**（11 files）。新增：`missions`(15)。
- `npm run e2e` → **10 passed**（不變；本階段無 UI/scene 接線，故無新 e2e，見下）。

## DoD 對帳
- [x] 任務 data model + airspace seam：`missions.js`（純邏輯）+ `airspace-taipei.js`（資料，B1 seam）。四型 enum `MISSION_TYPES`。
- [x] 四型判定正確：`inLandmarkRing`（光圈距離容差）、`advanceRings`（依序穿、不可跳號）、`checkAltitude`（above/below/band）、`isTakeoffLandingDone`（跑道落地 或 `forced_landing_success` 接 v1.1-2）。
- [x] 適應性挑選（純 JS）：`pickNextMission` 先近後遠、先大後小；上一題失敗→回填最簡單；雙人各自獨立（各帶 doneIds）；持續供題迴圈不卡（迴圈模擬測試證明）。
- [x] rings 取自既有 `rivers.js` 折線（`ringsAlongRiver(points, count)`，非硬編座標）。
- [x] facts / prompt / 鳥瞰知識 **全標 DRAFT**（`draft:true`，測試強制檢查每一筆）；facts ≤20 字。
- [ ] （本階段純邏輯/資料，無獨立 HITL；併 v1.1-5）

## 一輪四型 trace（必要 trace）
- 地標尋寶 `find_daanPark`：飛進大安森林公園光圈（dist≤280）→ 達成。
- 穿圈航線 `route_tamsui`：`ringsAlongRiver(淡水河折線,5)` 生 5 圈 → 依序穿（index 0→5）→ 達成。
- 高度挑戰 `alt_above101`：高度 ≥520m → 達成；`alt_lowriver`：≤120m → 達成。
- 起降練習 `practice_landing`：回松機落地（`landed_runway`）或真實模式迫降成功（`forced_landing_success`）→ 達成。
- 循環：`pickNextMission` 先近後遠先大後小供題，達成標 done → 下一題，全清回 null（迴圈不卡，測試覆蓋）。

## 設計決定（附「為何」）
- **瀏覽器級「任務循環」e2e 留 v1.1-4**：本階段刻意不接 UI/scene（handoff 硬性約束「UI 在 v1.1-4」）。沒有 UI 就沒有「接到任務」的畫面可驅動；用 vitest **迴圈模擬**（pick→done→pick→…→null）證明「循環不卡」的邏輯正確性，不寫依賴不存在 UI 的假 e2e。瀏覽器循環 e2e 在 v1.1-4 連 UI 後補。
- **判定函式全收純參數**（座標/高度/事件），不碰 scene/flight：missions.js 零相依、可單測；v1.1-4 再把 targetId→scene 座標、rings→rivers 折線、event→落地/迫降事件接起來。
- **全靜態、無生成**（端側無模型）：DRAFT 內容皆手寫靜態，標記待 Sung 校稿（沒有三件套安全網就不上生成，教訓 B4 反向）。

## 新增/改動
- 新增：`src/display/missions/missions.js`、`src/display/missions/airspace-taipei.js`、`tests/missions.test.js`。
- 改：`TEST_BASELINE.md`。（main.js/scene 不動 —— 接線是 v1.1-4）
