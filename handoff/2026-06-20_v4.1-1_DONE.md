# v4.1-1 離場流程 + pushback + 空中走廊 — 完成回報（agent 端）

> 對照交付：[`2026-06-14_v4.1-1_departure_pushback.md`](2026-06-14_v4.1-1_departure_pushback.md)。V4 子階段 **3／4**（v4.1 離場）。
> 含 Sung 2026-06-18 HITL 重塑：**ATR spawn-at-gate + 完整飛行週期空中指引**（離場 corridor + 進場 corridor）。

## 三閘（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **359 passed**（air-corridor×3、atc×4、context-keys depart 鍵；taxiway/flight 既有）。
- `npm run e2e` → **30 passed**（離場地面流程、離場越界、空中走廊；到場全鏈、GLB 載入 仍綠）。

## DoD 對帳
- [x] **登機短動畫（加油/行李 + 乘客）** — `ground-service.js`：登機時加油車/行李車（Kenney CC0）停飛機兩側；乘客＝ATC 計數器 `乘客 N/72`。⚠️ 乘客為計數器非走動 voxel 小人（見取捨1）。
- [x] **pushback 全自動 + remote 確認鍵** — scripted 拖車把飛機從 gate 推到 apron 接點、轉向 taxi 方向（smoothstep）；拖車視覺在機鼻。**遙控器「✅ 確認後推」鍵**＝`BTN.CONFIRM` + context-keys `DEPART_KEYS`，display 於「登機完成」廣播 `depart` 子模式換鍵、確認/結束還原（Enter/dev hook 仍可；逾時 8s 自動，不卡關）。
- [x] **taxi 到跑道頭 + 起飛排序** — 綠線+跟我車導到 RWY10 等待點；hold-short 排序 ATC「前面有一架在離場，稍候」→ 許可 cleared → 進跑道對正推油門 → 起飛。⚠️ 排序為計時(SEQ 5s)，可見的「前面那架」環境離場機留 polish（見取捨2）。
- [x] **【Sung redirection】空中走廊（完整航班空中段）** — 起飛 → `air-corridor.js` traffic pattern：爬升出場→右轉橫風→下風邊→左轉基腿→對正跑道10→五邊進場→通過跑道頭落地（接到場地面鏈）。半透明穿越環 + 每 leg ATC「松山離場/近場/進場」。
- [x] **【Sung redirection】spawn-at-gate** — ATR 從 gate 出生走完整離場（T-34C/F-16 維持跑道 spawn 快起飛）；**正解 6/18 回報的「綠線在支線、飛機在跑道」錯位**（移除「閒置 ATR 導最近門」反向 fallback）。
- [x] **vitest + e2e + typecheck 全綠**（上方附數）。
- [ ] **Sung 雙真機 HITL（本階段優先）** — pushback 懂不懂、自動引導不無聊、遙控器確認鍵順手、排序排隊感、空中穿越環跟得上。待真機。

## ⚠️ 取捨（明示，非降級）
1. **乘客＝ATC 計數器**（非走動 voxel 小人）：加油/行李車是實體（地勤車），乘客以「N/72」呈現；走動小人留 V6 polish。
2. **起飛排序＝計時 hold-short + ATC**（功能完整：等待→許可）；**可見的「前面那架」環境離場機未做**（需 transient 環境機 entity + scripted 起飛）。排隊「感」由 ATC + 等待提供；可見機留 polish/補做。
3. **空中走廊不強制高度**：給穿越環（目標高度標在 ATC），孩子自己飛過；不鎖姿態（保街機自由手感）。
4. pushback 拖車/地勤車＝重用 follow-me.glb（Kenney sedan）造型；專屬拖車/油罐車模型留 polish。

## 新增/改動檔案
- 新：`scene/air-corridor.js`（pattern 航點/推進/ATC，純函式）、`scene/corridor-markers.js`（穿越環視覺）、`scene/ground-service.js`（地勤車/拖車）、`scene/atc-phraseology.js`（見 v4.1-2）。
- 改：`main.js`（spawn-at-gate + 離場狀態機 + updateDeparture/updateArrival 分派 + updateAirCorridor + 遙控器 depart 子模式）、`input/keyboard.js`（Enter 確認）、`flight-model.js`（Input.confirm typedef）、`shared/protocol.js`（BTN.CONFIRM）、`remote/context-keys.js`（DEPART_KEYS）。
- 測：`tests/air-corridor.test.js`、`tests/atc.test.js`、`tests/context-keys.test.js`、`e2e/flight.spec.js`（離場流程/越界/空中走廊；#16/#17/#18 改離場框架）。

## 之後
- v4.1-2 收尾（ATC P1+P2 已併本批）：整合一趟完整航班 + 回歸 + 民航機 unlock（暫可選）+ DONE。
- 待 Sung：v4.1-1 雙真機 HITL；取捨2（可見環境離場機）拍板補或留。
