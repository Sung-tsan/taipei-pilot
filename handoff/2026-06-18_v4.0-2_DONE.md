# v4.0-2 到場流程（arrival）— 完成回報（agent 端）

> 對照交付：[`2026-06-14_v4.0-2_arrival_flow.md`](2026-06-14_v4.0-2_arrival_flow.md)。V4 子階段 **2／4**。
> P1→P4 逐階段 commit（`08fea26`/`96529c2`/`bba3493`/本次）。**到場地面全鏈實作完成、三閘綠**；6 歲 GO/NO-GO 待 Sung 真機。

## 三閘（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **351 passed**（taxiway 18：脫離道選取×4、gate 指派×2、停妥判定×2、runwayCoord、exitParallel…）。
- `npm run e2e` → **29 passed**（+1：到場全鏈「落地→脫離→指派門→停妥靠橋」以 dev hook + teleport 驗階段機）。

## DoD 對帳（到場地面全鏈）
- [x] **落地→脫離→taxi→指派門→靠橋·停妥全鏈通** —— 階段機 `none→exit→taxi→parked`：
  - **P1 落地→脫離**：落地依滾行方向選「前方」脫離道（`selectArrivalExit`，6 歲不回頭）→ 綠線+跟我車帶出跑道（機身→脫離道→平行道）。
  - **P2 taxi→指派門**：塔台**指派** gate（`assignArrivalGate` 輪派，每次到場停不同門）→ 導到指派門（非最近門）。
  - **P3 靠橋·停妥**：`isParkedAtGate`（門框內+nose-in朝向+速度≈0）→ 收綠線、空橋伸出(~1.2s)、`landingChime` Settle、ATC「已靠橋」。
- [x] **停妥判定 + Settle；空橋接出** —— 見 P3；空橋＝`GroundNav.dock()` 單盒延伸（voxel 風，§11）。
- [x] **vitest + e2e + typecheck 全綠**（上方附數）。
- [ ] **Sung 雙真機 HITL（本階段優先 GO/NO-GO）** —— 「6 歲落地→脫離→跟導航滑到 gate→停妥」順不順，待真機。

## ⚠️ HITL 中途回饋（2026-06-18，Sung 真機）→ 重塑後續
Sung 試玩回報，導引應涵蓋**完整飛行週期**，非只到場地面：
- **離場**：從航廈閘口 → 引導到跑道頭 → 起飛 → 空中離場指引。
- **到場**：空中進場航線指引 → 落地 → 引導到航站閘門（＝本階段已做的地面段）。
- **拍板**（Sung）：① ATR（民航）**從 gate spawn**、做完整離場流程（T-34C/F-16 維持跑道 spawn 快起飛）；② 順序＝**先收掉到場（本 DONE）**，再做「離場地面 + 空中進/離場 corridor」區塊。

## 已知缺口 / 取捨（明示）
1. **自由飛「閒置 ATR 在跑道上 → 導到 gate」是反的**（落地以外，飛機在跑道＝要起飛非到場）——
   產生 Sung 看到的「綠線在支線、飛機在主跑道」錯位 + 越界冒煙。此路徑＝v4.0-1 自由飛地面導航（e2e #17/#18 依賴它）。
   **不在本階段 band-aid**（會破既有 e2e、且與下一區塊重疊）；由**「spawn-at-gate 離場」區塊**正解（ATR 不再閒置於跑道）。
2. **綠線幾何本身無座標 bug**：`nodeWorld` 與滑行道鋪面同框對齊（已驗算）；錯位純為「飛機在跑道(lateral 0)、路線在平行道(lateral −90)」的 framing 問題。
3. 空橋＝單盒延伸（夠讀；精緻造型待 V6 polish）。ATC＝佔位文字（真 phraseology v4.1-2）。

## 新增/改動檔案
- `scene/taxiway.js`：`selectArrivalExit`/`exitParallel`/`runwayCoord`/`assignArrivalGate`/`gateParkPose`/`isParkedAtGate`（純函式，vitest 直測）。
- `scene/ground-nav.js`：`dock`/`undock`/`docked` + JetBridge 延伸動畫。
- `display/main.js`：到場階段機（exit/taxi/parked）+ 停妥靠橋 + dev hook `__tp.arrival`。
- `tests/taxiway.test.js`（+10）、`e2e/flight.spec.js`（+1 全鏈）、本 DONE。

## 之後
- **下一區塊（Sung 拍板）**：spawn-at-gate 離場（gate→跑道頭，並正解上方缺口1）+ 空中進場/離場 corridor 指引。
- 待 Sung：v4.0-2 6 歲 GO/NO-GO 真機；flight(v4) 重力/失速手感 HITL（POLISH_BACKLOG §4）；4 機型候選換入拍板。
