# v2.0 空戰 HITL 修正 — 完成回報（agent 端）

> Sung 雙真機測 v2.0-2 後回報 7 點。本輪修掉其中 6 點（#4 競速屬尚未實作的 v2.1-1，記為設計拍板待該階段做）。
> 部分項目原列 v2.0-3/-5（瞄準框/小地圖/外型音效）→ **提前做以救回可玩性**（HITL 服務產品，不被階段切割綁死）。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **272 passed**（24 files；baseline 246 → +26）。新增：`minimap`(16)、`missile-model`(3)、`plane-collision`(4)、`dogfight`(+3：有限氣球/cleared、飛彈外型、nearestBalloon)。
- `npm run e2e` → **16 passed**（空戰測加驗 🎈計數 + 小地圖 + 瞄準框顯示；RWD 回歸在 remote 版面改動後仍綠）。

## 逐項對帳（Sung 7 點）
1. ✅ **氣球放大 + 計數 + 指引**：氣球 mesh ×3.2（~13m，找得到）；HUD 顯示 `🎈{剩}/{總}`；沒鎖到時 TaskSlot 出「最近氣球」方向箭頭+距離；**改成有限一輪**（打完整輪 → `cleared` 事件 + 慶祝音 + 換新一輪），不再無限個別重生 → 知道「射完了沒」。
2. ✅ **瞄準框**：每視口一個 `.reticle`，平常置中；鎖定（飛近、射程+錐角內最近目標）後**投影到目標螢幕位置自動追瞄**（變紅）；射程外不鎖＝置中不追。
3. ✅ **飛彈外型 + 音效**：擬真彈（空對空/空對地）改用 `missile.js` 飛彈 voxel、順速度方向旋轉；命中音＝爆炸（`explode`）；卡通彈維持亮球 + 啵（Sung 說卡通版 OK）。
4. ⏸ **競速起點/終點 A→B**：競速（v2.1-1）尚未整合進遊戲 → 記為拍板：競速要有明確起點/終點或「先到 A 再到 B」的指引（見 ROADMAP §4 v2.1 註記）。該階段做時落實（race.js 已備兩型；缺的是 UI 指引）。
5. ✅ **手機按鈕位置**：context 動作鍵（喇叭/發射）從中欄移到**左欄、靠起落架下方、整欄靠底排**（`#leftCol` justify flex-end）→ 左拇指好按。RWD 短螢幕回歸仍綠。
6. ✅ **角落小地圖**：`minimap.js`（北朝上雷達，右下角）顯示友機（朝向三角）+ 空戰氣球；**任何模式**都開（不只空戰）→ 知道彼此方位距離。
7. ✅ **兩機相撞後果**：新增 `plane-collision.js` 純判定；主迴圈每步檢查兩機 3D 距 <18m → **安全＝幽靈穿透不處罰；溫和＝❤️−1；真實＝冒煙→再撞墜毀**（接後果軸，含 1.5s 冷卻防連觸）。

## ⚠️ 取捨/說明
- **瞄準框/小地圖提前做**（原 v2.0-5）：HITL 指出沒它們「亂發射/不知道在哪」→ 救回可玩性必須現在做。v2.0-5 仍可再 polish（鎖定動畫、敵機 blip 顏色等）。
- **小地圖北朝上、共享一個**：雙人分屏共用，無法各自 heading-up → 北朝上是唯一一致解；超出世界半徑的 blip 夾到邊緣環。
- **氣球有限一輪**取代無限重生：給「打完」的明確完成感；打完自動換新輪（永遠有靶）。
- 兩機相撞半徑 18m、冷卻 1.5s 為手感初值，HITL 可調。

## 新增/改動檔案
- 新增：`src/display/ui/minimap.js`、`src/voxel/models/missile.js`、`src/display/flight/plane-collision.js` + 各自 test、本 DONE。
- 改：`src/display/combat/dogfight.js`（氣球放大/有限/cleared/指引/飛彈外型+朝向/targetPos）、`src/display/main.js`（瞄準框投影 + 小地圖 render + 兩機相撞 + 命中音分流 + 氣球指引）、`src/display/audio.js`（沿用 explode/balloonPop）、`index.html`（reticle + minimap 元素 + CSS）、`remote.html`（發射鍵移左下）、`tests/dogfight.test.js`。

## 待 Sung 複驗（HITL）
- 氣球放大後好找嗎？計數/指引箭頭夠清楚？瞄準框追瞄手感？飛彈外型像不像？小地圖看得懂方位？左下發射鍵好按？兩機相撞（溫和/真實）受損/爆炸合理？
- 接著回 v2.0-3 PvP（玩家互打＝命中接後果軸：冒煙重生/出局）。
