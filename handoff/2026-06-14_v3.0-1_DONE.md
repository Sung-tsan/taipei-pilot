# v3.0-1 天氣系統核心 — 完成回報（agent 端）

> 對照交付：[`2026-06-13_v3.0-1_weather_engine.md`](2026-06-13_v3.0-1_weather_engine.md)。V3 子階段 **1／4**。
> 接 V2 收尾（`taipei-pilot-v2.0` 已 tag）後 V3 起手。**套件先行**（工法基準新規）：天氣 render 用內建/three 原生，不自幹 shader。

## 三閘全綠（附數）
- `npm run typecheck` → **0 errors**。
- `npm test` → **291 passed**（25 files；+10 weather：profile/後果閘/roll/壞 rng 安全網）。
- `npm run e2e` → **21 passed**（+1：安全恆晴 fog 遠、設霧 fog 拉近、設雨粒子顯示）。

## DoD 對帳
- [x] **晴/多雲/雨/霧可切**：`weather/weather.js`（純邏輯）+ `weather/weather-render.js`（modulate world.js）。
- [x] **`weatherProfile` schema（9 機場 ready、台北數值、其餘 DRAFT）**：`WEATHER_PROFILES`——**tsa（松山）數值已定**（晴.50/雲.28/雨.14/霧.08）；台中/高雄/花蓮/台東/金門/馬祖/澎湖/綠島 8 個 **draft:true**（schema 完整、值 DRAFT，V5 航網活化；金門/馬祖霧機率已埋招牌傾向）。
- [x] **隨機 roll + 後果軸三檔閘**：`gateProbs`——安全＝恆晴；溫和＝只晴/多雲（雨霧歸 0 重正規化）；真實＝整張全開。`rollWeather` 依閘後機率抽，壞 rng 退晴（安全網）。進場/換後果模式即 roll（`applySettings`）。
- [x] **render modulate world.js（霧/雨/雲），雙視口一致**：`WeatherRenderer` 改 `scene.fog`(near/far/色)+背景色+`sun`/`hemi` 光照倍率+`clouds` 顯隱+`skydome` 染灰 + **THREE.Points 雨**（跟焦點飛機移動）。天氣是全域 state、兩視口共用同一 scene → 一致。光柱（MeshBasic fog:false）不被霧吃（沿用既有）。
- [x] vitest + e2e + typecheck 全綠（附輸出）。
- [ ] **Sung 雙真機 HITL（併 v3.0-4）**：台北天氣好看、霧中靠回家箭頭/光柱導航可行、安全模式恆晴。**dev/HITL 預覽：display 按 `C` 循環 晴→多雲→雨→霧。**

## 套件先行（工法基準新規落地）
- 霧＝內建 `THREE.Fog`（world.js 既有，只調參）；雨＝**three 原生 `THREE.Points`** 粒子（非自寫 shader/粒子引擎）。
- 評估過 `three/addons` Sky/Water（可用、零新 dep）；本階段 handoff 明示「modulate 既有 world.js 不重造」→ 故沿用既有漸層天空 + Fog，Sky/Water 留 v3.0-3 生活感/水面再評估。
- 結論：v3.0-1 不需新 npm dep，全用內建 + three 原生。

## ⚠️ 取捨（明示）
1. **8 個非松山機場 weatherProfile 為 DRAFT**（schema ready、值佔位）：符合 handoff「9 機場 schema、台北數值、其餘 DRAFT、V5 活化」。
2. **天氣＝rolled + 後果閘，無玩家選天氣 UI**（符合 spec：天氣是氛圍 + 任務驅動）；天氣挑戰任務覆寫＝v3.0-4。dev/HITL 用 `C` 鍵預覽。
3. **側風/亂流/受損%＝v3.0-2、日夜＝v3.0-3、天氣任務/音效＝v3.0-4**（本階段只 weather state + 視覺）。

## 新增/改動檔案
- 新增：`src/display/weather/weather.js`（純邏輯）、`src/display/weather/weather-render.js`（render）、`tests/weather.test.js`、本 DONE。
- 改：`src/display/scene/world.js`（skydome 命名供 modulate）、`src/display/main.js`（WeatherRenderer + roll/apply + 每幀 update + C 預覽 + __tp）、`e2e/flight.spec.js`。

## 下一步
- v3.0-2 側風/亂流 + 受損百分比（**含 V3 最大未知數 GO/NO-GO：暈感**；flight-model 加外力輸入；v1.1-1 damage 擴成 damagePct）。
